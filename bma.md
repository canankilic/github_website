---
layout: default
title: BMA Calculator
---

Below is the Python code used to calculate the BMA index:

```python
weight = int(input("Enter weight = "))
height = int(input("Enter height = "))

bma_index = weight / (height * height)

print("Your bma index is ", bma_index)
```


